import React from 'react';

function AddNotePage() {
    return (
        <section>
        <h2>Add Note page</h2>
        <p>Nothing to see here.</p>
    </section>
    );
}

export default AddNotePage;